{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -w run db:migrate:deploy && pnpm -w exec turbo run build --filter=@itfin360/web",
  "outputDirectory": ".next"
}
